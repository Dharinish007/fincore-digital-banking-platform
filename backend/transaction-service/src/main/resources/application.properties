spring.application.name=transaction-service
server.port=8083

# ---------- H2 in-memory DB (default - zero setup, just click Run) ----------
spring.datasource.url=jdbc:postgresql://localhost:5432/fincore
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=root123
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
services.account-service.url=http://localhost:8082/api/v1/accounts

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# ---------- MySQL (uncomment + comment out H2 block above to switch) --------
# spring.datasource.url=jdbc:mysql://localhost:3306/fincore_transactions?createDatabaseIfNotExist=true
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=yourpassword
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
