# Transaction Atomicity - Angular Frontend

This workspace contains a complete Angular frontend scaffold for the Transaction Atomicity module.

Quick start:

1. Install dependencies:

```bash
npm install
```

2. Run development server:

```bash
npm start
```

This app uses Angular, Angular Material and Reactive Forms. All pages work with dummy data provided by `TransactionService`. When ready, connect `ApiService` to real backend endpoints.
